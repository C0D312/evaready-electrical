# Phase 3E2 Feature Validation

## Exact Scope

Starting feature commit: `2b0087f0e5f3a76fa10ee47803ca604e471b9553`.
Implementation: `7e76f10ef1ef0fb62e46cfc98f76f0c43241b9ff`.
Final tested implementation, including the fixture portability correction:
`026f6d8bd8b9611f2a5bd259db9c3839f0bd0079`.
Branch: `codex/whole-site-completion`.
Main remains `e6197fcd00747ae86cabfff675516176c9e66ec6`.

This records feature validation, not deployment or whole-website perfection.
The accompanying JSON contains the implementation file list, exact command
arguments, output hashes, dependencies and retained-evidence identities.
This document does not claim to contain its own final commit SHA.

## Clean Validation

A fresh detached checkout of the final tested implementation began clean,
without dependencies or generated build output. Node 22.23.1 ran `npm ci`, both
full and production-only `npm audit`, ESLint, a production GitHub-preview build,
TypeScript, nineteen static audits, all 27 audit-unit files and register drift.
All 27 command steps passed. Both dependency audits reported zero vulnerabilities.
The complete audit-unit result is **1,522 passed, zero failed, skipped or cancelled**.

Preview configuration: target `github-preview`, base path
`/evaready-electrical`, site URL `https://c0d312.github.io/evaready-electrical`.
The local build marker identifies the tested implementation exactly. Its legacy
`mainCommit` field is the build SHA; it does not mean main was updated.

- 1,001 sitemap routes and 1,004 HTML outputs accounted for; zero critical
  page-health or static visibility warnings.
- Metadata, internal links, visible/schema consistency, claims/offers,
  conversion markers and static-export payload coverage passed.
- All 873 suburbs remain index/follow, self-canonical and included in sitemap;
  zero noindex pages or redirects. All 873 indexation decisions remain unreviewed.
- Zero approved location evidence records, referenced assets or orphan assets.
  The privacy result covers the empty scoped evidence dataset, not all repository
  content, Git history or complete PII detection.
- Register drift passed: 1,001 reviewed/rewritten rows, 895 publication-pending
  and 106 historical live-verified rows. The 21 reviewed rewrites and separate
  `/services` catalogue delta remain pending with null live SHAs.

Generated audit files stayed in the isolated checkout and were not staged.
The generated search index has no Git content diff; its status reflects line
endings only. The original sealed local export and protected owner checkout
were preserved.

## Retained Browser Evidence

The fixed 4,459-case inventory is accepted with zero pending cases. Its original
classification is 3,647 freshly executed and 812 retained cases. **None is claimed
as fresh execution during this exact-commit continuation.** Existing failed
attempts remain retained separately and contribute zero acceptance.

| Existing profile | Accepted | Original fresh | Original retained |
| --- | ---: | ---: | ---: |
| Desktop Chromium 1440 | 1,297 | 1,181 | 116 |
| Desktop Firefox 1440 | 1,297 | 1,181 | 116 |
| Desktop WebKit 1440 | 373 | 257 | 116 |
| Mobile Chrome 390 | 373 | 257 | 116 |
| Mobile Safari 390 | 373 | 257 | 116 |
| iPad 768 | 373 | 257 | 116 |
| iPad Pro 1024 | 373 | 257 | 116 |

The final clean checkout matches 108 sealed production/test inputs (byte identity
or Git LF/CRLF conversion only), all 1,004 HTML visible/SEO/schema surfaces,
103 images, sitemap and robots. Ten dependency versions match. The original
1,159 supporting evidence files have verified hashes. Original finite interaction
limits, natural focus and observation-only restoration assertions remain intact.
The accepted contained runs retain their request reconciliation; no public
browser, real form submission or genuine conversion was executed in this phase.
Local emulation is not physical-device proof. No new Lighthouse, field Core Web
Vitals or INP measurement is claimed.

## Failures Preserved and Repaired

Five historical register tests used stale publication/count/hash expectations.
The repair retains genuine historical snapshots and original historical hashes,
compares all 979 unaffected current rows directly, and independently asserts the
21 approved deltas and catalogue-only change. Actual rows are not projected or
normalised to conceal changes. An initial new expectation incorrectly assumed
six historical claim states were held; that failed run remains documented.

The first clean commit run then found a fixture byte-hash mismatch caused solely
by Git converting 15,976 LF endings to CRLF on Windows. The pinned original LF
hash and every card/link/copy/SEO assertion remain unchanged. Two additional
regression tests reject content changes and non-Git whitespace changes. The
first clean run (1,497 passes and one file-level failure) is not counted as passed.

The original 74-file candidate passed its staged configured secret/contact scan.
The subsequent two-file correction's precommit scanner could not access Git
under the sandbox identity; its exact committed blobs were scanned successfully
under normal approval before push. This is recorded as a committed-blob scan,
not a successful precommit scan. No security setting was modified.

## Remaining Holds and Delivery Boundary

Documentary specialist credentials, security/cabling/air-conditioning/solar scope,
serviceability, overlapping-route business decisions and indexation owner
decisions remain held. Register review status does not verify those claims.
No fabricated review figures, jobs, evidence or commercial data were introduced.

Only normal feature-branch delivery is authorised. A later exact-SHA main release
needs direct owner approval and separate artifact/live verification. No main
update, PR, merge, workflow dispatch, deployment, domain/hosting change or external
account operation is included. Artwork was preserved. Previously authorised
navigation, Quote, focus and footer changes are included in the implementation;
this register/clean-validation continuation changed no public website code.

Reports use only the owner's exact supervisor conversation in the Codex in-app
browser, subject to normal permissions. No native ChatGPT app or alternate relay
is used. Delivery failure must never be bypassed.
