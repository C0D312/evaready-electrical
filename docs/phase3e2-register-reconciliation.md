# Phase 3E2 Register Reconciliation

## Scope and Diagnosis Before Implementation

The authorised supervisor phase was received through the exact in-app browser conversation on 15 September 2026. Repository and feature HEAD remain `2b0087f0e5f3a76fa10ee47803ca604e471b9553`; fetched main remains `e6197fcd00747ae86cabfff675516176c9e66ec6`. No release is authorised.

The original failed TAP is retained locally with SHA-256 `5a5d50945706a7584d458207a6b43f1615968adf0879e11619adf1e262b69797`: 1,401 passing and five failing tests. No failure is relabelled as a pass.

- Phase 3D6 publication expects `/services` to remain live-verified. Its two changed derived catalogue descriptions now require publication-pending/null live SHA; the other historical released pages remain unchanged.
- Phase 3D6 isolation expects `10671b2d398a2a8d8a3cb564df3d8e9a00dce9d85284c3b34929e6640e5e2a10` for 930 historical rows; actual was `0d6546a456c37ec53c297033508831bb93acd7022fc0c3b23c2d6b966c15fe5f`.
- Phase 3D7 expects 21 pending/980 reviewed and 128 live/873 pending. The authorised 21 reviewed rewrites and catalogue publication hold require 0 pending/1,001 reviewed and 106 live/895 pending. Its nine fault-guide publication assertions still pass.
- Phase 3D8 isolation expects `33bc3c011c20244a6bce5712c6e47de4f43c765dbb1746966c17fda2cefe98ec` for 939 historical rows; actual was `c41fe5d5858dcc6565ef7ddabb6569967b11fd1c9cb2233c10a328f0a9e4fa84`.
- Phase 3D9 isolation expects `90877fc29a6c2d89501a3b313814be2a2fcfef0228c29ed14f004200da68d6e1` for 945 historical rows; actual was `7e32d803ed0e5659d47b611b30ec20fe6645cb15c54236a519ba266b83b694ae`.

Independent read-only inspection of committed register `aa58901770136b68df02b445c8b393e4f932d463` reproduced all three original historical hashes exactly, plus the unchanged historical suburb hash. The sealed pre-3E2 register matches committed `2b0087f0e5f3a76fa10ee47803ca604e471b9553`. The existing comparator confirms all 979 other rows unchanged. These are stale temporal expectations, not evidence of a production register defect; current output still requires the independent assertions below.

## Safer Test Design

The rejected assert-then-project approach remains prohibited and unapplied. This design never rewrites or normalises actual rows to resemble historical values.

Two test fixtures are byte-for-byte exports of committed `reports/whole-site-completion-register.json`, not generated by the current production code:

| Fixture | Source commit | SHA-256 |
| --- | --- | --- |
| `phase3d9-publication-register.json` | `aa58901770136b68df02b445c8b393e4f932d463` | `5490c08b9ed3eb218106b5eda8998ae81bc148dd8699b9449afca5caeffb40cd` |
| `phase3e2-before-register.json` | `2b0087f0e5f3a76fa10ee47803ca604e471b9553` | `6d012198ddbbb6c1ff384b0266f852d291a5340cb4f842a5bbcc2ad6e98d20a8` |

Historical release assertions retain their original hashes and run on the genuine frozen publication snapshot. Current-behaviour tests still run the current generator. An independent explicit 21-route contract compares all 979 unaffected rows directly against the pre-3E2 fixture, preserves identity fields, checks every approved field/hold and the separate catalogue-only change, and verifies publication-pending/null live SHAs. Mutation tests reject false publication, removed holds, changed source identities, unapproved claims and route corruption. Expectations do not import the generator's route selection or evidence-hold function.

The 4,459 accepted interactions remain retained only while relevant source/export identities match. Guard40 passed again before these changes. Register tests and fixtures do not change public output. Final validation and feature delivery are still pending; this document is not a release or launch-completion claim.

## First Focused Run

The first new focused run recorded 201 passes and six failures. All six arose from one incorrect new test expectation about the immutable pre-3E2 fixture: the six consolidation candidates historically had `claimOwnerEvidence: automated-only`, while the fifteen specialist routes were already `held`. Current output correctly requires `held` for all 21. The independent contract now asserts those distinct historical states and the stricter current state explicitly. The failed TAP remains retained; none of its failures count as acceptance.

Fixture integrity permits Git's LF/CRLF checkout conversion only. JSON values, fields, ordering and original LF-content hashes remain protected. Actual register objects are never normalised or projected.

## Working-Candidate Validation

- Focused six-file suite: 207 passed, zero failed/skipped/cancelled.
- Full 27-file audit unit suite: 1,520 passed, zero failed/skipped/cancelled.
- Full ESLint and TypeScript `--noEmit --incremental false`: passed.
- Register generation comparison: 1,001 sitemap routes, no drift.
- Existing semantic isolation comparator: passed, including all 979 protected rows and the exact metadata/catalogue exceptions. Result SHA-256: `ab416da52fc8634804edc0d732a4e7bc357f8f39c3f7cab2c12643e3477365be`.
- Original interaction ledger integrity: 1,159 unique supporting files verified, zero hash mismatches. The original 4,459 accepted cases retain their original fresh/retained classifications; none is claimed as a fresh execution today.
- Configured source credential/contact scan: 72 files, zero findings. Separate staged-file review remains required. This is not a claim of complete PII detection or a history-wide scan.

Commands used Node 22.23.1 with `NEXT_PUBLIC_DEPLOYMENT_TARGET=github-preview`, `NEXT_PUBLIC_SITE_URL=https://c0d312.github.io/evaready-electrical` and `NEXT_PUBLIC_BASE_PATH=/evaready-electrical`:

```text
node --import tsx --test --test-concurrency=1 tests/audits/phase3d6-fault-guides.test.ts tests/audits/phase3d7-fault-guides.test.ts tests/audits/phase3d8-core-pages.test.ts tests/audits/phase3d9-location-pages.test.ts tests/audits/phase3e2-register-contract.test.ts tests/audits/whole-site-completion-register.test.ts
node --import tsx --test --test-concurrency=1 [all 27 tests/audits/*.test.ts files, expanded and sorted by PowerShell]
node node_modules/eslint/bin/eslint.js
node node_modules/typescript/bin/tsc --noEmit --incremental false
node --import tsx scripts/generate-whole-site-completion-register.ts --check
```

The separate exact-commit validation record must identify its tested SHA. No document can identify its own final commit SHA in advance, and these working-candidate results are not a clean-commit or deployment claim.

## Clean Checkout Line-Ending Defect

The first isolated validation of implementation `7e76f10ef1ef0fb62e46cfc98f76f0c43241b9ff` passed installation, both npm audits, lint, production build, TypeScript and all nineteen static audits. Its full unit run recorded 1,497 passes and one file-level failure before the 23 card-link cases could execute. Git's Windows checkout converted the immutable card-link fixture's 15,976 LF endings to CRLF: raw SHA-256 changed from `70e09ae94b071da14dda3d51c0f3ab9f7d0373b239fb45cd16f1c4b5cf610c3a` to `779ebdfee8d15ce921178d8a1125c73f9346741de41865e9542b78d7ae7164b4`. Converting CRLF back to LF reproduces the original hash exactly.

The narrowly scoped test correction retains the original pinned LF hash and every functional assertion. Two added regression cases accept LF/CRLF only and reject changed content, trailing whitespace and bare-CR conversion. The fixture and production code are unchanged. The failed clean run is preserved, not reported as passing. A separate exact-commit rerun is required before feature push.

The first clean build separately matched the accepted 108 source/test inputs (byte-identical or Git line endings only), all 1,004 HTML visible/metadata/schema surfaces, 103 images, sitemap and robots. This supports retention of existing contained browser evidence; it is not new browser execution or a deployment claim.
