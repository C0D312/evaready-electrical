# The public website uses no Google Maps or Places browser credentials.
# Future Google Business Profile OAuth credentials must stay in an
# owner-controlled private sync environment and must never be committed here.
